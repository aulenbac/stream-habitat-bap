---
title: "FAIR Instream Tributary Habitat Metrics"
output: 
  flexdashboard::flex_dashboard:
    social: menu
    source_code: embed 
runtime: shiny
---

```{r global, include=FALSE}
library(flexdashboard)
library(shiny)
library(datasets)
library(ggplot2)
data<- read.csv("Data/All_Data.csv") 

```

Column {data-width=200 .sidebar}
---------------------------------------------------------------------------------
```{r, include=FALSE}
n= names(data)

selectInput("Metric", "Metrics", n[7:length(n)-1] )


```
Column {data-width=800}
------------------------------------------------------------------------------------
```{r}
ggplot(data, aes(x=Grad, y=input)+ geom_point()+facet_wrap(~Program))

```



```{r}

#shinyApp(
#  ui = fillPage(fillCol(flex = c(NA, 1), inputPanel(selectInput("Metric", "Metric:", choices = colnames(data))),
#      plotOutput("Metirc", height = "100%"))
#  ),
#  server = function(input, output) {
#  output$Plot <- ggplot(data, aes(x=Grad, y=input)+ geom_point()+facet_wrap(~Program))
 #   })
#  },
#  options = list(height = 600)
#)

```

